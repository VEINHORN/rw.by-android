<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="AppTheme.LogIn" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@color/loginBackground</item>
        <item name="android:editTextBackground">@color/loginEditTextBackground</item>
        <item name="android:editTextColor">@color/loginEditTextColor</item>
        <item name="android:textColorHint">@color/loginEditTextHintColor</item>
        <item name="colorButtonNormal">@color/loginButtonColor</item>
        <item name="android:textColor">@color/loginButtonTextColor</item>
    </style>
</resources>
